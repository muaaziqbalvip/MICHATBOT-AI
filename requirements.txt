# ═══════════════════════════════════════════════════════════════════════════
# Vercel Gateway dependencies ONLY (api/index.py)
# This is a lightweight router — it forwards requests to the GitHub Actions
# engines over HTTP. It does NOT load any AI models itself, so it must NOT
# contain torch/transformers/diffusers — those would blow Vercel's 500MB
# function size limit.
# ═══════════════════════════════════════════════════════════════════════════
fastapi==0.110.0
uvicorn==0.28.0
requests==2.31.0
pydantic==2.6.4
